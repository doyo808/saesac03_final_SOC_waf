services:
  waf:
    image: owasp/modsecurity-crs:nginx
    container_name: waf
    restart: unless-stopped
    ports:
      - "192.168.40.5:80:8080"
    environment:
      - BACKEND=http://192.168.40.10:80
      - PARANOIA=1
